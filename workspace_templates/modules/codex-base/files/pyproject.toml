[project]
name = "{{PROJECT_NAME}}"
version = "{{TRADINGCODEX_VERSION}}"
requires-python = ">=3.11,<3.15"
dependencies = [
  "django>=5.2,<5.3",
  "django-ninja>=1.4,<2",
  "pydantic>=2",
  "pytest>=8"
]

[project.scripts]
tcx = "tradingcodex_cli.__main__:main"
