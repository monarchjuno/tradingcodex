interface:
  display_name: "TCX Workflow"
  short_description: "Coordinate operate-plane investment workflows"
  default_prompt: "Use $tcx-workflow to route this TradingCodex workflow through the compact lane and selected team."
policy:
  allow_implicit_invocation: true
